programa {
  funcao areaRetangulo(real base, real altura){
  real area

  area = base * altura

escreva("Área do retãngulo: ", area, "cm²")
  } 
  funcao inicio() {
    areaRetangulo(5.0,5.0)        // real base = 5.0 
                                  // real altura = 5.0

                                  // areaRetangulo(base,altura)
  }
}
